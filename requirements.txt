# ============================================================================
# ForgeQA 依赖清单 —— 运行所需
# ============================================================================
# 安装：
#   pip install -r requirements.txt
#
# 版本号为 2026-09-22 实测通过（Python 3.13.12 / macOS arm64）的组合。
# 全部要求 Python >= 3.10，与本工程 pyproject.toml 的 requires-python 一致。
#
# 想要更宽松的版本范围而不锁死，用：
#   pip install -e ".[ui,db,demo]"
#
# ---------------------------------------------------------------------------
# 安装后必做一步：下载 Playwright 浏览器内核
# ---------------------------------------------------------------------------
#   python -m playwright install chromium
#
# 这一步最容易漏。requirements 只装 Python 侧的 playwright 包，
# 浏览器内核要单独下载，否则 UI 用例会报 "Executable doesn't exist"。
# ---------------------------------------------------------------------------


# ---- 核心运行时（必需）------------------------------------------------------
# 接口调用、YAML 用例解析、造数
requests==2.34.2
PyYAML==6.0.3
Faker==40.39.0


# ---- UI 自动化（用例中含 ui 任务时需要）-------------------------------------
# 纯接口/DB 回归场景可以不装这两行，省去浏览器下载
playwright==1.63.0
Pillow==12.3.0


# ---- 数据库（接 MySQL / PostgreSQL 时需要）----------------------------------
# SQLite 走 Python 标准库 sqlite3，零额外依赖，不装这一段也能跑
# MySQL       -> SQLAlchemy + PyMySQL（本文件已含）
# PostgreSQL  -> 额外装 psycopg2-binary 或 pg8000
SQLAlchemy==2.0.54
PyMySQL==1.2.3


# ---- 内置演示站点（可选）----------------------------------------------------
# 只用于 forgeqa demo 起本地演示站；接真实站点后可删
Flask==3.1.3
