# ============================================================================
# ForgeQA 开发依赖 —— 在 requirements.txt 基础上追加测试与覆盖率工具
# ============================================================================
# 安装：
#   pip install -r requirements-dev.txt
#
# 跑测试：
#   PYTHONPATH=. python -m pytest tests -q            # 192 个单测
#   PYTHONPATH=. python -m pytest tests --cov=forgeqa # 带覆盖率
# ============================================================================

-r requirements.txt

# ---- 测试 -------------------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
