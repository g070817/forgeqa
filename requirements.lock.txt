# ============================================================================
# ForgeQA 依赖锁定 —— 完整闭包，含全部传递依赖
# ============================================================================
# 用途：在另一台机器上 1:1 复现 2026-09-22 验证过的环境。
#
#   pip install -r requirements.lock.txt
#
# 实测基线：Python 3.13.12 / macOS arm64
#   192 个单测通过 + 端到端套件 5/5 通过
#
# 与 requirements.txt 的区别：
#   requirements.txt       只列顶层依赖，由 pip 解析传递依赖（范围宽松）
#   requirements.lock.txt  连传递依赖一起钉死（完全可复现）
#
# 日常开发用 requirements.txt；要复现/排查环境差异时用本文件。
#
# ⚠️ 别忘了：python -m playwright install chromium
# ⚠️ 本闭包按 macOS/Linux 计算。若在 Windows 上安装，pip 会自动补上
#    colorama 与 tzdata（两者都是平台条件依赖，故未列在此处）。
# ============================================================================


# ---- 项目直接依赖 -----------------------------------------------------------
requests==2.34.2
PyYAML==6.0.3
Faker==40.39.0
playwright==1.63.0
Pillow==12.3.0
SQLAlchemy==2.0.54
PyMySQL==1.2.3
Flask==3.1.3

# ---- 测试工具 ---------------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.16.1


# ---- 传递依赖 ---------------------------------------------------------------
certifi==2026.7.22
charset-normalizer==3.4.9
greenlet==3.5.6
idna==3.18
pyee==13.0.1
typing-extensions==4.16.0
urllib3==2.7.0
blinker==1.9.0
click==8.4.2
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
